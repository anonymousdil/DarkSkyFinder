# API Configuration
# Note: These APIs are optional and the app will work with mock data if not configured

# 7Timer! API - No API key required (free service)
# Used for astronomical weather forecasting
# Documentation: http://www.7timer.info/doc.php

# AQICN (World Air Quality Index) API
# Primary and exclusive source for real-time Air Quality Index (AQI) data
# Get your free API token at: https://aqicn.org/data-platform/token/
# Sign up and navigate to your API token page
# Free tier includes: 1,000 calls/minute with rate limits
VITE_AQICN_TOKEN=your_aqicn_token_here

# OpenAI API Configuration (Required for LLM-powered conversational features)
# Get your API key at: https://platform.openai.com/api-keys
# Used for: Conversational chatbot capabilities, natural language query processing
# Cost: GPT-4o-mini is used for cost efficiency (~$0.15/1M input tokens)
# IMPORTANT: This is backend-only and will NOT be exposed to the frontend
OPENAI_API_KEY=your_openai_api_key_here

# Backend Configuration
# Port for the LLM backend server (default: 3001)
BACKEND_PORT=3001
# Backend URL for frontend to connect to (default: http://localhost:3001)
VITE_BACKEND_URL=http://localhost:3001

# Note: The app uses 7Timer! by default which doesn't require an API key
# Add the AQICN API token above to enable:
#   - Real-time AQI data from AQICN (World Air Quality Index)
#   - Detailed pollutant measurements (PM2.5, PM10, O3, NO2, SO2, CO)
#   - Air quality data for any location worldwide
#   - Forecast data for pollutants with daily averages and ranges

# Add the OpenAI API key to enable:
#   - Free-form conversational queries with the Stary chatbot
#   - Natural language understanding for astronomy questions
#   - Context-aware responses about stargazing and celestial events

